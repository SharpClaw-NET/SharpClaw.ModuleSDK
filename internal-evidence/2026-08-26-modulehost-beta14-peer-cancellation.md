# ModuleHost Beta14 Peer Cancellation Evidence

This record covers the ModuleHost beta14 correction in `D:\Source\SharpClaw-NET\SharpClaw.ModuleSDK`. The source commit is `05a94eeb8802f9d80616afd0f6cd4663657f3901`. Local `main`, `origin/main`, and the remote `main` reference this commit. The source worktree has no tracked modifications. Three pre-existing untracked `${task}` paths remain outside this change.

The package version is `0.5.0-beta.14`. The public dependency identities are SharpClaw.Contracts `0.5.0-beta.29` from source `45a9d9ceac876373b111e1abc481b41d72de50d4`, with DLL SHA-256 `65817E8ACE02EF0487069E6C2721F5255B925868B7D155C8A6CB87A94B07CCE6`, and SharpClaw.Core `0.5.0-beta.22` from source `fd08edbd44d2bc26749be7d2a6213988ed9f2254`, with DLL SHA-256 `B274500D8E094A32B4DA21147FBEC744D178395F6F80124BC4E85D83CBC5CA17`. Core exact-pins Contracts beta29.

The correction uses the existing capability cancellation frame for `SidecarCrossSidecarActionEntryPeerCancellation`. The host signs a cancelled terminal authority from the authenticated relay, source parent call, receiving peer call, relay binding hash, cancellation timestamp, canonical authority hash, and control-token proof. The receiving module validates the complete envelope and consumes the relay through the public Contracts beta29 transition. The module rejects replay and advances its local sequence without a terminal execution. The target terminal path keeps one incoming-terminal record and one-use state. The module observes the authenticated peer sequence before target terminal storage calls. Disposal uses safe cancellation for concurrent call cleanup.

The focused CrossSidecar Release filter passed 7 tests with zero failures and zero skips. The regression covered a blocked target dispatcher, a target storage call, signed pre-terminal cancellation, binding rotation, and later cross-sidecar use. The target storage gateway received the expected module identity, storage name, and operation. The build used task-root output under `D:\temp\SharpClaw.ModuleSDK\beta14-contracts-beta29-core-beta22-20260826`.

The next verification gate will run all current OutOfProcess Release tests, all ModuleSDK Release tests, and all InProcess Release tests. It will then run package identity and tools allowlist checks, a package-only consumer without project references, and a separate-process cancellation, storage, nested, cross-sidecar, and rotation smoke. The retained Agent archives will be used only when their exact hashes are available. No package has been packed or published. Overwatch review remains required before publication.

This record contains no credentials, temporary configuration, archive, log, cache, or generated output. The related ignored report is under `docs/internal/codex`.
