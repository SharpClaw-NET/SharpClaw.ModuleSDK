# ModuleHost Beta14 CompleteCall Contract Blocker

This bounded turn audited the rejected `SharpClaw.ModuleHost.OutOfProcess` beta14 completion path. The required result is a supported fail-closed transition after Contracts rejects a non-retryable `CompleteCall`. The host must preserve the original completion error, release the local and Contracts action state, avoid carrier cleanup errors, and define safe later-session behavior.

The authoritative source state is `de2fa2bee82c0e086ef8e7a0c020962046bda3b7` on `main`. Local `main`, `origin/main`, and remote `main` identify this commit. The source correction used for the rejected candidate is `3aab2add668032aec688158295e63d5fd7436e12`. The rejected candidate is `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826\candidate-final\SharpClaw.ModuleHost.OutOfProcess.0.5.0-beta.14.nupkg` with SHA-256 `6A3A4212C730C912A045C549CB56174BD1D49C322D67049A2E28F28CCA5FFFF`. It remains unpublished and must not be reused.

The current host calls `SidecarCapabilitySession.CompleteCall` from `CompleteOutgoingCapabilityCallAsync`. A rejected result removes only `_outgoingCapabilityCalls`. The outer carrier cleanup then calls `CompleteHostActionEntryCarrier` while Contracts still owns the active action call and its HostEntry state. That cleanup can replace the original error and leave the Contracts call active. The ModuleSDK source cannot clear that state by removing its local record.

The exact public Contracts beta29 assembly was loaded from `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826\public-deps\contracts\lib\net10.0\SharpClaw.Contracts.dll`. Its SHA-256 is `65817E8ACE02EF0487069E6C2721F5255B925868B7D155C8A6CB87A94B07CCE6`. Reflection reported public `SidecarCapabilitySession.CompleteCall(System.Guid, System.Int32)` and public `SidecarCapabilitySession.Disconnect()`. It reported no public `AbandonCall`, `AbortCall`, `CancelCall`, or equivalent per-call action cleanup operation. The only public session-wide cleanup operation is `Disconnect`, so it cannot preserve later use of the same session.

This is an external Contracts blocker. A ModuleSDK-only local removal would bypass Contracts state. A session disconnect would terminate all capability state and would not provide the required per-call completion behavior. The next accepted Contracts boundary must provide an authenticated per-call action abandon or abort transition that validates the exact call and carrier lineage, clears action and terminal state, releases nested descendants, and returns a bounded result that the host can process without replacing the original failure.

No source file, package version, package archive, or published feed changed during this turn. The previously reported Release suites remain `13/13`, `3/3`, and `140/140` with zero failures and zero skips. The required live-session regression was not added because the accepted Contracts API cannot express the required cleanup. The exact Agent beta3 archives and compatible IntegratedGate runner also remain absent from the ModuleSDK task evidence, so the required downstream gate is not available.

The tracked provenance correction is in `internal-evidence/2026-08-26-modulehost-beta14-completecall.md`. This report is also recorded under the ignored `docs/internal/codex` path at `docs/internal/codex/2026-08-26-beta14-completecall-contract-blocker.md`. No credentials or regenerable files were added to the repository.
