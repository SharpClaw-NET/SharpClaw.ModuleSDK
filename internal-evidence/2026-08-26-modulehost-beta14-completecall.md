# ModuleHost Beta14 Completion Evidence

The objective corrected HostEntry completion in `SharpClaw.ModuleHost.OutOfProcess`. The host now keeps the outgoing root call tracked until Contracts accepts `CompleteSessionCall`. It waits for nested and cross-sidecar descendants through the existing state machine. It returns other Contracts completion errors without retrying the action. The correction keeps one capability session, one dispatcher, one storage path, and one authenticated action exchange.

The first source commit is `4e52d5d7c86771c892e8f540ff030851e9376c00` with subject `fix(out-of-process): await host action completion`. The second source commit is `21057d459ff8f2f2de7831edab28a9f9e933c2ab` with subject `test(out-of-process): declare permission hook in cross-sidecar fixture`. The second commit contains the final source and test state. Local `main`, `origin/main`, and the remote `main` reference the second commit. The tracked tree is clean. The existing untracked task output paths were preserved and were not staged.

The regression `AgentsJobImportNestedPermissionCompletesBeforeRootCarrierAndKeepsSession` starts an Agents root HostEntry call. It performs six prior calls. It invokes the module-owned Permission child. It verifies two dispatcher calls, two terminal calls, one storage write, empty pending carrier state, binding rotation, and later session use. The cross-sidecar fixture declares the Permission hook required by the shared test module.

The focused HostEntry regression passed one test with zero failures and zero skips. The additional nested, real dispatcher, and active rotation filters passed three tests with zero failures and zero skips. The complete OutOfProcess Release suite passed `138` tests with zero failures and zero skips. Its TRX is `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-contracts-beta29-core-beta22-20260826\results-full-2\outofprocess-full-2.trx` with SHA-256 `61FB8DE3CF23DE599FFF64A1C244F11E58EF09E9A2CFE0E9D4FE4AC20EBE8394`. The complete ModuleSDK Release suite passed `13` tests with zero failures and zero skips. Its TRX SHA-256 is `A14A4F6516135FAFCEEA9E9FBB4F4DD2E1B64D73199954DAA032B73E241FB2C9`. The complete InProcess Release suite passed `3` tests with zero failures and zero skips. Its TRX SHA-256 is `2F04BBFD61A3AA8A7CDC3FB662B1C268D9ED94951687F8AD3C181A5FAB753E9F`.

The focused HostEntry TRX SHA-256 is `83D90FFE5E90FDBE7D31278A940FC8913B19B4F445F90A9B3FED1118B5E06DE2`. The three-filter focused TRX SHA-256 is `6CFFAC42E13A654357E9514385D8B50F422F65C145C2BAB4616698D88D109751`. The test-root setup failure was rerun with `SHARPCLAW_MODULESDK_TEST_ROOT` set. The first VSTest result created in the source checkout was moved to the task root. No source test output remains in the checkout.

The candidate is `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-contracts-beta29-core-beta22-20260826\candidate\packages\SharpClaw.ModuleHost.OutOfProcess.0.5.0-beta.14.nupkg`. Its length is `1114254` bytes. Its SHA-256 is `EDD7581C9F5F575B2617BEF54EC84CD9024EF11F9C93D5755D507FDE9332875F`. The package ID is `SharpClaw.ModuleHost.OutOfProcess`. The package version is `0.5.0-beta.14`. The license is `AGPL-3.0-or-later`. The project and repository URL is `https://github.com/SharpClaw-NET/SharpClaw.ModuleSDK`. The repository commit is `21057d459ff8f2f2de7831edab28a9f9e933c2ab`.

The candidate depends on `SharpClaw.ModuleHost.InProcess 0.5.0-beta.9`, `SharpClaw.Contracts 0.5.0-beta.29`, and `SharpClaw.Core 0.5.0-beta.22`. The public dependency DLL hashes are `A95A5DD6B7B01EB757D0D3847EE4B335DEB8771CF49C01A322A7584061C32E64` for InProcess, `65817E8ACE02EF0487069E6C2721F5255B925868B7D155C8A6CB87A94B07CCE6` for Contracts, and `B274500D8E094A32B4DA21147FBEC744D178395F6F80124BC4E85D83CBC5CA17` for Core. The accepted SDK beta9 DLL hash is `B5B7C66FE3E4940C2F5B1E388C971C0EA3D5BDCE8779539457C24A52CF63110E`.

The package library and tools OutOfProcess DLLs both have SHA-256 `78ADFF88F13E33C8727B9FF34B90299E4E70E63AC82209D97EE18DC3BE679DDC`. The tools payload contains exactly eight entries. The payload contains `SharpClaw.Contracts.dll`, `SharpClaw.Core.dll`, `SharpClaw.ModuleHost.InProcess.dll`, `SharpClaw.ModuleHost.OutOfProcess.deps.json`, `SharpClaw.ModuleHost.OutOfProcess.dll`, `SharpClaw.ModuleHost.OutOfProcess.exe`, `SharpClaw.ModuleHost.OutOfProcess.runtimeconfig.json`, and `SharpClaw.ModuleSDK.dll`. The executable hash is `43410D20D39E3CCDAC5A21C7B07CCFCBBC3C455E67F606F1B3D1C5F1AE42504`. The dependency file hash is `6BCB804DD3A47442D7333F2630F11F7CB3620D11FEF09328CEE5790533395386`. The runtime configuration hash is `41442586A8262BC96A1C80F82F4D9384C03BFD465D221CBF01FB3563170F9E65`. The tracked `eng/Verify-PackageAssemblyIdentity.ps1` gate passed for the accepted SDK and InProcess packages and the candidate.

The package-only consumer used a PackageReference to the candidate and no project references. It restored from the task-local feed and public NuGet.org. The Release build completed with zero errors. The direct consumer run exited with code zero and printed `0.5.0.0` and `Hello, World!`.

The direct extracted-tools smoke started the packaged executable in a separate process. It returned HTTP 200 readiness and discovery responses. It reported `readiness={"moduleId":"application_smoke_module","authorized":false}`, `discoveryLength=13250`, `discoveryHasApplicationSmoke=True`, `processRunningBeforeStop=True`, `processExit=-1`, `stdoutBytes=1848`, and `stderrBytes=0`. This smoke proves packaged startup and discovery. It does not prove an authenticated action.

The retained three-sidecar runner was copied from `D:\temp\SharpClaw.ModuleSDK\beta14-contracts-beta29-core-beta22-20260826\agent-gate\runner` to the current task root. The exact beta14 host payload replaced the runtime files in the copy. The gate started three sidecar processes and reached authorization and capability exchange endpoints. It produced no action result or TRX within the bounded five-minute run. The task stopped the gate and confirmed that no runner process remained.

The retained runner is not a compatible proof for the current dependency graph. Its `SharpClaw.Tests.deps.json` identifies `SharpClaw.Contracts 0.5.0-beta.25` and `SharpClaw.Core 0.5.0-beta.18`. The task-owned Agent source copy identifies Contracts beta26, Core beta19, Agents beta5, Context beta4, and TwoTierPermission beta4. The current candidate requires Contracts beta29 and Core beta22. The exact accepted Agent beta3 package archives were not available in the retained ModuleSDK evidence for a compatible rebuild. The three-sidecar gate is therefore an external integration blocker, not a ModuleSDK source result.

Known build warnings are existing NU1903 warnings for `System.Security.Cryptography.Xml 10.0.7`, existing nullable warnings, and an existing unused exception warning in `OutOfProcessModuleCapabilityTransport.cs`. No new warning class was identified. No package was published. Publication remains blocked until Overwatch accepts this exact source and candidate and a compatible Agent gate proves the production flow.

All regenerable evidence remains below `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-contracts-beta29-core-beta22-20260826`. No credential, cache, archive, log, or temporary configuration was added to the tracked tree. The next bounded turn requires a compatible exact Agent beta3 gate or an explicit Overwatch disposition.
