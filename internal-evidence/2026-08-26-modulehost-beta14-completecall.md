# ModuleHost Beta14 Completion Evidence

This bounded objective repairs OutOfProcess HostEntry completion after Contracts rejects a parent completion while a descendant remains active. The host now keeps the outgoing root call in local tracking until Contracts accepts `CompleteSessionCall`. A non-retryable completion error releases local tracking before the error reaches carrier cleanup. The existing single capability session, dispatcher, storage path, and authenticated action exchange remain unchanged.

The pushed source commit is `3aab2add668032aec688158295e63d5fd7436e12` with subject `test(out-of-process): isolate cross-sidecar counters`. Local `main`, `origin/main`, and remote `main` all identify this commit. `git diff --check` passes. The tracked tree has no changes. Three pre-existing untracked task paths remain in the checkout and were not staged or changed: `${task}/`, `SharpClaw.ModuleHost.InProcess/${task}/`, and `SharpClaw.ModuleHost.OutOfProcess.Tests/${task}/`.

The production correction is in `SharpClaw.ModuleHost.OutOfProcess/OutOfProcessCapabilityHostSession.cs`. `CompleteOutgoingCapabilityCallAsync` calls `CompleteSessionCallResult` before local removal. It removes the outgoing call after an accepted result or a non-retryable result. It waits for nested descendants only for the exact Contracts active-nested error. `ReleaseOutgoingCapabilityCall` signals call state and requests rotation retry after local removal. The cross-sidecar fixture and regression use a module-owned Permission action without adding that action to the host catalog.

The focused Agents-to-Permission cross-sidecar regression passed `2/2` tests with zero failures and zero skips. The final complete Release suites passed `13/13` ModuleSDK tests, `3/3` InProcess tests, and `140/140` OutOfProcess tests. No suite reported a skip. The final TRX files are `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826\results-trx-modulesdk\modulesdk.trx`, `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826\results-trx-inprocess\inprocess.trx`, and `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826\results-trx-outofprocess\outofprocess.trx`. Their lengths and SHA-256 values are `21400` and `C09A38C6208D9072E40954964F7A1EE304FC848C279D278F3ABF0FC58CE3BE55`, `7656` and `9D2F47F725AB45807088C3CFBFD7198F591E1CDD3A5DF35BF7A9F08AB566D86C`, and `254865` and `5EAE9FD3C3D26C42D95C886B8B6053B959FADC2D5E9193B7508E8793C66D7364`.

The immutable unpublished candidate is `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826\candidate-final\SharpClaw.ModuleHost.OutOfProcess.0.5.0-beta.14.nupkg`. Its length is `1114091` bytes and its SHA-256 is `6A3A4212C730C912A045C549CB56174BD1D49C322D67049A2E28F28CCA5FFFF9`. The nuspec package ID is `SharpClaw.ModuleHost.OutOfProcess`, the version is `0.5.0-beta.14`, the license is `AGPL-3.0-or-later`, and the repository and project URL is `https://github.com/SharpClaw-NET/SharpClaw.ModuleSDK`. The nuspec repository commit is `3aab2add668032aec688158295e63d5fd7436e12`. The exact dependencies are `SharpClaw.ModuleHost.InProcess 0.5.0-beta.9`, `SharpClaw.Contracts 0.5.0-beta.29`, and `SharpClaw.Core 0.5.0-beta.22`.

The accepted public dependency package DLL hashes are `B5B7C66FE3E4940C2F5B1E388C971C0EA3D5BDCE8779539457C24A52CF63110E` for ModuleSDK beta9, `A95A5DD6B7B01EB757D0D3847EE4B335DEB8771CF49C01A322A7584061C32E64` for InProcess beta9, `65817E8ACE02EF0487069E6C2721F5255B925868B7D155C8A6CB87A94B07CCE6` for Contracts beta29, and `B274500D8E094A32B4DA21147FBEC744D178395F6F80124BC4E85D83CBC5CA17` for Core beta22. The public NuGet archive hashes used for staging are `63AD5D663163A910E1AFE98AC7B48A3DBBDC6F297FEEAAD0093E7F7ABBC7C2A7` for ModuleSDK, `9D7AE039739DE80DC352F37D110A4FF4665D82E29C0579201991F317A2FDE98B` for InProcess, `885B10510C43C5DAA09A12DD024FCAC8FF76AD4C3CF5E00E114A8E234A78CE0D` for Contracts, and `318F2C725862BC1DA4DC1EDA189928DA06D5E130FFAE40F7350E2C7C6A28F401` for Core.

The tools payload contains exactly eight entries: `SharpClaw.Contracts.dll`, `SharpClaw.Core.dll`, `SharpClaw.ModuleHost.InProcess.dll`, `SharpClaw.ModuleHost.OutOfProcess.deps.json`, `SharpClaw.ModuleHost.OutOfProcess.dll`, `SharpClaw.ModuleHost.OutOfProcess.exe`, `SharpClaw.ModuleHost.OutOfProcess.runtimeconfig.json`, and `SharpClaw.ModuleSDK.dll`. The payload contains no source, project, object, PDB, test, README, XML, or nested package entry. The package and tools OutOfProcess DLLs both have SHA-256 `5CB4ADB0A91D371B17A2D3E55BE908D47AC52753571996696CC76DA587FFE23C`. The embedded Contracts, Core, InProcess, and ModuleSDK DLL hashes match the accepted values above. The tracked `eng/Verify-PackageAssemblyIdentity.ps1` gate passed, and the explicit eight-entry allowlist gate passed.

The package-only consumer used one `PackageReference` and no project reference. It restored from the task-local candidate feed and public NuGet.org. Its Release build completed with zero errors. Its run loaded the packaged `OutOfProcessModuleServer` assembly and printed `0.5.0.0`.

The direct extracted-tools smoke ran the packaged `SharpClaw.ModuleHost.OutOfProcess.exe` in a separate process. Authenticated HTTP requests returned status `200` for readiness and discovery. Readiness returned `{"moduleId":"lifecycle_smoke_module","authorized":false}`. Discovery returned status `200` with length `3807`. The process stopped after the bounded check. This smoke proves packaged startup and discovery, but it does not prove an authenticated action.

The exact Agent beta3 archives with hashes `3AD95AE6A70DB8051DF77110393109377C9451E8C4096A969E37EB39436A62A4`, `7C0E28B9AEBF9BF0F31C105E5BD58BDFF4CF47D7A8CF86ABF904369659B724FE`, and `83B6D988A8326DBC61D2765613C08997F52051838759415F95EB0ADAF8EF7094` were not present under the ModuleSDK task evidence. The retained IntegratedGate runner at `D:\Temp\SharpClaw.AgentOrchestration\beta8-entry-correction\gate\IntegratedGate` was also absent. Therefore, the real two-sidecar Agents-to-Permission gate and the compatible three-sidecar `agents.job.import` gate remain external blockers. No Agent or SharpClaw source was inspected or changed.

Known warnings are existing NU1903 advisories for `System.Security.Cryptography.Xml 10.0.7`, existing nullable warnings, and an existing unused exception warning in `OutOfProcessModuleCapabilityTransport.cs`. No package was published. Publication remains blocked until Overwatch accepts this exact source and candidate and the compatible Agent gate is available.

All regenerable evidence is below `D:\temp\SharpClaw.ModuleSDK\beta14-completecall-unwind-20260826`. No credential, cache, archive, log, or temporary configuration was added to the tracked tree. The next bounded turn requires the exact Agent beta3 archives and a compatible IntegratedGate runner, or an explicit Overwatch disposition of that external blocker.
